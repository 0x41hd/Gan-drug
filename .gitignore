# ── Python ──────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.ipynb_checkpoints/

# ── Virtual environments ────────────────────────────────────────────────
venv/
env/
.venv/
ENV/
feedbackgan/

# ── Model weights & large binaries ──────────────────────────────────────
# Keep these OUT of git (too large). Share via Google Drive / releases instead.
*.h5
*.hdf5
*.weights.h5
*.pkl
*.pkl.gz
*.ckpt
*.pb

# But DO track the SA-score file if you want it versioned (uncomment to keep):
# !fpscores.pkl.gz

# ── Generated outputs ───────────────────────────────────────────────────
GAN_model/run_*/
GAN_model/base_run/
GAN_model/*/viz/
GAN_model/*/weights/
GAN_model/*/generated_data/
GAN_model/*/feedbackGAN/
experiments/
*.png
prediction_*.csv
testing_*.csv
generated_molecules.csv
training_epoch_time.txt
sampling_data.txt
grid_results_*.csv

# ── Keep these tracked ──────────────────────────────────────────────────
!framework.jpg

# ── OS / editor junk ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/